- CNPJEmpresa - Remover;
- exibirPeril - Alterar o nome do atributo errado no UML;
- remover a data do UML (ruim de trabalhar com isso agr e desnecessário);
- tirar o método de comentar (Vamos focar só na reação por enquanto);
- Criar Publicação como classe mãe e interação como classe filha, colocar 2 atributos (idPublicação como Integer, totalInteração como Integer);
- Criar um método publicar na classe Dev;
- Totalreação deve ser removido;

- Colocar os métodos login(u Usuario), 
logoff(u Usuario), 
excluir(u Usuario), 
cadastrar(u Usuario), 
exibirDados(cpfUsuario String), 
alterarDados(String cpfUsuario,String novoCpf,String email, String nome, String senha, String especialidade, String cnpj),
alterarDadosDev(String cpfUsuario,String novoCpf,String email, String nome, String senha, String especialidade),
alterarDadosInvestidor(String cpfUsuario,String novoCpf,String email, String nome, String senha, String cnpj),

------------------------------------
Resolver o problema com a remoção do usuário (ele não é removido no alterarDados);